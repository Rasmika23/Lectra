
  # Build Accessible Reusable Components

  This is a code bundle for Build Accessible Reusable Components. The original project is available at https://www.figma.com/design/GnsfkqT26D62AvKFcL79Y9/Build-Accessible-Reusable-Components.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  